---
status: active
authority: structure-memory
category: tutorial-support
last_reviewed: 2026-07-04
---

# Tutorial Support Structure

## Purpose

The tutorial support layer provides reusable authoring pieces for future tutorials without defining tutorial steps or scene content.

## Tutorial Upgrade Prototype (2026-09-18)

### Opening cinematic (2026-09-20)

- `PrototypeTutorialUpgrade` serializes a scene-local `PrototypeTutorialOpeningSequence` in the same source file. It runs before mission ticking and exposes `IsOpeningCinematic` so `PrototypeTutorialPromptView` hides overhead guidance until the cinematic finishes. No new manager, assembly, save state or runtime UI hierarchy is introduced.
- The existing scene gunner starts at (6,-2), surprises with a 0.45-unit visual hop and the existing SpeechBubble `!`, runs `GoblinGunnerShotRunner` once aimed at the landed player, then moves left to (0,-2) and north to its original (0,21) encounter position at 6 units/second (1.5x the original speed). The left leg follows the painted entrance corridor; Y=-4 is blocked by its lower wall. Motion uses the existing wall-aware motor. SpeechBubbleComponent is added only to this scene instance and references the existing bubble prefab; shared gunner/player prefabs are unchanged.
- `PlayerHubSpawnPresentation2D.TryPlayScriptedArrival` reuses authored Hub fall/spin/shadow/landing effects and holds the lying pose until the sequence allows wake. The opening now permits wake on actual contact from its designated projectile; the gunner repeats the hop and `!` after the player stands, then escapes. At 70% of total waypoint-path distance, the opening releases its input/protection, returns the camera to the player and fades out the letterbox over 0.25 seconds. The gunner continues the final 30%; IsEscaping gates its normal tutorial firing and arrival checks until it reaches the endpoint. It bypasses input/automatic wake and does not emit Hub completion or consume Hub progress. Ordinary Hub/portal arrival paths retain their existing recovery rules.
- `GoblinGunnerShotRunner.Run` / `GoblinGunner.FireProjectile` optionally report the spawned projectile to the sequence. Only that projectile binds `LightBeadProjectile2D.BindPresentationImpact`: contact with the designated player's resolved hurtbox consumes the shot and sends one notification without bypassing cinematic invulnerability. Unbound projectiles keep normal damage/trigger behavior. Miss/lifetime expiry reports a setup error and restores controls; cancellation clears the callback and destroys the owned shot.
- The opening fades HUD first, then acquires player protection immediately before starting arrival without another yield. This avoids Editor direct-start normalization releasing the token during letterbox startup and leaving the later None interaction state unowned. Protection remains through projectile-contact wake until the 70% handoff. It temporarily disables only the gunner AI during the shot because spawn-idle suppression cancels pattern runners. The existing spawn-idle pause remains in place for the later tutorial. Cancel/player loss/disable cancels the shot and motion, hides the bubble, restores the visual offset and arrival state, then releases only its own protection/letterbox. Completion is one-shot per director instance; interrupted playback can restart on rebind.
- The scene root `TutorialOpeningCameraPoint` is the Inspector-authored initial camera center (default (8,-6,0)); move its Transform X/Y to frame the opening. The director references it through `opening.openingCameraPoint` and owns the focus session until handoff/cancel. Scripted arrival can opt into external camera ownership, so fall/landing/wake do not override this framing. Normal Hub/portal cameras are unchanged.
- Validation: `Tools/Validation/TutorialOpeningRegression.cs` exercises the native arrival hold, explicit wake, normal portal auto-wake and cancellation/disable restoration; `Tools/Validation/TutorialOpeningSceneValidation.py` checks references and floor clearance along the escape route. Full-scene visual Play Mode acceptance is still needed for surprise timing, shot visuals and the entire cinematic handoff. This remains a scene-specific prototype, with no Architecture/Contracts promotion proposed.

### Existing mission flow

- Camera query fix (2026-09-20): `CameraBootstrap.GetMainCamera/GetPlayerCamera/GetBrain/GetLegacyFollow` now return cached rig references when available. Re-running rig setup on every read rebound Follow/LookAt to the player and reset priority; the arrival start-height camera query therefore erased the opening focus immediately after it was assigned. Initialization when absent and explicit scene/player-registration rig setup remain unchanged. `Tools/Validation/CameraFocusQueryRegression.cs` guards repeated query target/priority preservation.

- [PrototypeTutorialUpgradeScene.unity](../../../Assets/_Project/Scenes/PrototypeTutorialUpgradeScene.unity) is the title-launched tutorial entry as of 2026-09-19. TitleProfileSlotService sends both new profiles and unfinished-tutorial resumes here. It replaces TutorialCorridor at the same Build Settings position; the old scene remains on disk. The authored portal continues to DarkLord_Tutorial, followed by the existing hub completion flow. Completed profiles still target ProtoTypeHub.
- `TutorialUpgrade_Blockout` follows the user's reference diagram: start room at (8,-4), left side entry into a narrow vertical dash lane, shared attack/skill/chest/weapon-pad room at (0,29), north approach and boss room at (0,51). The old seven-room content group names remain for stable authored references; attack, skill, chest and swap groups share the same room origin. `ReferenceLayoutGeometry` retains the six blockout floor rectangles and disabled outer BoxCollider2D references. Actual wall collision now belongs to TutorialTileGrid/Wall. Spawn is (8,-6), movement goal (0,-1), dash goal (0,18.5); the latter leaves standing clearance below the dash gate at Y=19.75.
- It reuses the existing player spawner, camera prefabs, GlobalUIRoot, service wiring, and TutorialDefaultWeaponBootstrap from TutorialCorridor. The old introductory cutscene, input prerequisites, combat gates, tutorial persistence events, and outgoing portal are not copied. Shared prefabs and bootstrap code are unchanged.
- Hold attack uses the same actual GoblinGunner as the dash encounter; skill practice uses four dynamically spawned GoblinWarriors with their existing prefab AI. Previous dummy instances remain inactive in the scene for reference. The chest uses the existing TreasureChest prefab and its normal loot behavior. The swap room currently has two placement pads and pickup anchors, not assigned weapon pickups.
- [PrototypeTutorialUpgrade.cs](../../../Assets/_Project/Runtime/Features/Tutorial/PrototypeTutorialUpgrade.cs) owns five prototype missions: reach the movement goal; reach within 2.6 units of the gunner (no recorded dodge-hit requirement); land nine confirmed basic attacks (the hold instruction remains); defeat four full-charge targets, then three Q targets; select and confirm chest loot. Each basic hit confirmed against the gunner increments the /9 counter by one, once per execution token regardless of held input. Combo index is irrelevant; restarting the combo, releasing input or cancelling recovery with dash never revokes confirmed progress (2026-09-19 revision). Chest confirmation advances to an inventory-open lesson; a successful fresh InventoryToggle press after the chest closes advances the portal objective. The authored portal at (0,51) reuses ScenePortal's visuals and TutorialScenePortal entrance/state-preservation behavior, targeting the enabled DarkLord_Tutorial scene.
- The scene's chest is visible from entry and held locked by the director's pending reservation on its scene-added ChestMonsterKillLock. Stage 4 releases that reservation after skill completion. [PrototypeChestNavigation.cs](../../../Assets/_Project/Runtime/UI/Tutorial/PrototypeChestNavigation.cs) listens only to that chest's successful `OpenedUi` event and binds the `ChestScreen` showing the same inventory. Its scene-authored overlay intercepts input during reveal, then shades everything except the first visible item and shows the Mouse1 glyph above it. Only a right click inside that slot forwards the existing selection operation. No hierarchy is created by this guidance at runtime.
- `ChestScreen` exposes optional owner-scoped guided selection plus a successful `SelectionCommitted` event; unowned screens keep normal behavior. While guiding, direct selection/confirmation and reroll holds are gated, the overlay blocks pointer actions, and existing `UiStackPlayback` ownership plus saved `EventSystem.sendNavigationEvents` block keyboard UI actions. After selection, all guidance restrictions are released; the confirm graphic pulses between its authored normal and pressed colors using unscaled time. A failed commit does not complete the tutorial. A successful commit calls `CompleteChestTutorial` (stage 5). Close/disable/rebind cleans up ownership, navigation and tint. Reopening after a learned selection preserves free operation and resumes the confirm cue.
- The chest navigation is a scene-specific prototype, not a general chest redesign. Its state is not persisted. Review spotlight alignment and confirm tint in the full scene across resolutions; the isolated [PrototypeChestNavigationRegression.cs](../../../Tools/Validation/PrototypeChestNavigationRegression.cs) exercises selection and ownership without starting the full game bootstrap.
- Chest guidance presentation follows ItemDetailPanel's current values: unscaled OutCubic fade/slide from Y=-24 over 0.12 seconds and back down while fading over 0.1 seconds. The four shades fade without moving. Selection releases input immediately while visuals finish exiting; only then is the shield object hidden. Disable/reopen cancels presentation immediately to prevent stale overlays. Confirm tint switches directly between authored normal/pressed colors every 0.55 seconds, without interpolating intermediate colors. Existing scene-authored Graphics are driven directly; no new runtime hierarchy or shared ItemDetailPanel changes.
- `TutorialTileGrid` contains Ground (layer 7, Background sorting) and Wall (layer 30, Wall sorting/order 1), matching ShadowCorridor. The user-painted GrandHall/lobby tiles and half-unit grid transform are retained. Wall owns a merged TilemapCollider2D, static Rigidbody2D and synchronous CompositeCollider2D; Ground has no collider. The 22 former OuterWall BoxCollider2D components are disabled, so painting/erasing Wall tiles changes the actual collision boundary. Shared tile assets are unchanged.
- The scene-authored GoblinGunner waits at (0,21). Its normal targeting/burst AI is held in spawn-idle pause for this encounter; the scene director emits one pooled projectile downward every 1.5 scaled seconds, using existing goblin projectile art. Eighteen authored projectile transforms provide the pool; no projectile hierarchy is created at runtime. Existing swept contact, GAS dash invulnerability, slowdown and zoom are retained. Firing continues until arrival; reaching the gunner front starts retreat to (0,27) through AbilityMotionController2D and the existing wall-aware MovementMotor2D, at up to 4 units/second. After the first basic hit, it walks between nearby points around the retreat marker at 65% of its current MoveSpeedFinal; the existing motor retains wall collision ownership.
- A scoped damage filter protects the gunner until retreat ends and nine confirmed basic hits complete. The director then calls RequestDeath and instantiates four warrior prefabs at (-3,28), (3,28), (-3,32), (3,32). Shared monster prefabs/AI are unchanged; tutorial monsters suppress loot drops.
- Each warrior DeathStarted event schedules replacement after one scaled second while stage 3 remains active. Dead objects follow normal Enemy death cleanup; replacements are new instances, never reactivated corpses. All four slots count toward both existing quotas (four full-charge kills and three Q kills). Other attacks can kill but earn no skill progress. A separate credited flag handles DeathStarted preceding HitConfirmed and prevents duplicate progress. Qualifying skill hits are deliberately lethal.
- Skill completion changes stage before killing survivors, prevents further replacements, and releases the chest lock. The final legacy gate stays closed through inventory stage 5 and opens at stage 6. The scene director owns spawned instances and death subscriptions and destroys/releases them on disable. This encounter is intended for one run per scene load; it does not resurrect a dead authored gunner on component re-enable.
- The existing TutorialPlayerHealthAutoRecover restores HP after 0.05 seconds and guards lethal damage during the four missions. Scoped cooldown multipliers reduce the two starter skills to about one second without changing AbilityDefinition assets. Completion releases cooldown handles and health protection; player unregistration/disable releases player events and cooldown handles. Component disable also disposes the damage filter and hides bullets. No tutorial progress is persisted.
- The existing left `QuestHudView` hides its tutorial main objective, resolving the owner once per active scene; the above-player prompt projects `ProgressText`. It suppresses unrelated officer objectives in this prototype. Gameplay owns all progress. The scene-authored `PlayerTutorialPrompt` uses [PrototypeTutorialPromptView.cs](../../../Assets/_Project/Runtime/UI/Tutorial/PrototypeTutorialPromptView.cs) to follow the current player through screen-space projection, showing a current objective and mapped glyphs. Four Images show movement bindings; other stages use the existing primary glyph Image. The initial starting-room movement prompt (stage 0), cinematic dash cue (stage 1 only while IsDashPromptVisible), basic attack stage 2, skill stage 3 and chest stage 4 show the overhead view. Dash guidance follows the existing ZoomIn/Waiting camera sequence and hides during dash execution/zoom-out. Later forward/portal movement, inventory stage 5 and open popups hide the view. PromptInstruction supplies the caption after the bound glyph; basic/charge/Q instructions include hold/release semantics and current/required counts. The existing starter sword and its exact basic/charge/Q definitions are the fixture's expected loadout.
- [PrototypeTutorialEntranceGuide.cs](../../../Assets/_Project/Runtime/UI/Tutorial/PrototypeTutorialEntranceGuide.cs) projects a bound action onto an authored SpriteRenderer and fallback TMP label, subscribing to InputBindingService.BindingChanged while enabled. Six authored Presentation children are stage-bound: movement 0, basic attack 2, both skills 3, chest 4, portal 5. The six fixed floor/entrance guide roots (including dash entry) are now inactive; their authored references are retained. Text/fallback labels use the existing Galmuri9 blackOutline material; attack backing is removed and old room labels are inactive. Skill guides use ItemDetailPanel-style unscaled EaseOutCubic alpha/vertical slide (open .25s, close .15s, hidden world Y offset -.45; transition frame retains the starting pose even after a long encounter-spawn frame). The controller remains active while its content is hidden and reads gameplay-owned Stage; it never changes progress. No UI hierarchy is created at runtime.
- On first eligible bullet approach (above the player, within 2.7 units and 0.48 horizontally), the director starts at 60% world speed, displays the mapped dash glyph and instructions immediately, and zooms to 65% lens size over 0.9 unscaled seconds. World speed follows current projectile distance instead of elapsed time: 60% at 2.7 units down to an 8% minimum approach speed, then full pause at the 0.95-unit safety boundary. Intro projectile travel clamps to that boundary even across a long frame. The trigger uses the requested 2.7-unit distance directly, without an extra predictive allowance; offset trajectories use the circular distance boundary. Zoom continues while stopped if the player entered close to the bullet. Fresh dash input is accepted throughout slowdown and the stopped wait; a key held before the cue must first be released. Confirmation switches the same owner to 0.15 combat slow motion and activates the real GAS dash while authored bullets remain still. Normal bullet time resumes after dash completion; zoom restores from its actual current size over 0.25 seconds, including early confirmation. The quest text is `회피하며 전진하자.` and arrival completes the objective regardless of recorded invulnerable contact; the intro does not repeat within the scene instance.
- The Core `ITimeScaleRampBackend` / `TimeScalePausePlayback.SetOwnedTimeScale` path lets this director update only its own scale atomically in Infrastructure `TimeScalePauseService`. The strongest owner still wins and repeated updates retain the original restore scale; legacy Acquire remains non-overwriting. Finite scales are clamped to 0–1. Full pause from another owner suspends the approach ramp.
- The director uses owner-scoped UI, weapon, dash and swap input blocks during the intro. Its UI block is released before GAS activation because UIManager also applies control-lock tags; rejected activation reacquires it and retains the current ramp/stop. Another owner's pause is never released. Player loss/disable restores camera and releases remaining ownership, while preserving the one-shot intro state. Cleanup checks ownership before releasing an already-finished time request. The pre-cue dash press block begins on player binding and remains until the guided dash completes. Player release/disable clears this owner even before the cue; rebind reacquires the pre-cue block. Dodge stage 1 uses world +Y, with normal movement/aim direction in later stages.
- Weapon pickup assignment/swap training remains outside this encounter slice. `DemonKing_VisualPreview_NoCombat` still disables its gameplay scripts, colliders and Rigidbody2D simulation through scene overrides. Other unused anchors remain for later authoring.
- Failed-dash fade/reset remains outside this slice; failed dodges retain the existing retry behavior. The unassigned-prefab branch of the director still supports the older dummy regression fixtures (see PrototypeTutorialEncounter backlog). Layout validation checks enclosure, goal access, gate bypasses and references; encounter validation checks real prefab/spawn/chest wiring and serialized tile palette counts. Full scene Play Mode remains necessary for AI movement, sprite sorting and presentation acceptance.
- Shared quest presentation has a prototype-only projection; shared prefabs, bootstrap and asset schemas are unchanged. Main-scene Play Mode and visual acceptance remain pending; build, isolated regression and serialized-reference checks are recorded in the [session log](../../SessionLogs/2026-09-18.md). Existing Editor direct-start gameplay policy still applies, including development run setup. No Architecture/Contracts promotion is proposed for this scene-specific prototype.

## Current Structure

### Playable DarkLord tutorial encounter (2026-09-18)

- Tutorial defeat item scatter (2026-09-20): the existing zero-HP callback invokes `PlayerDeathReturnToHub2D.PrepareForScriptedDeathPresentation` on the disabled normal-death component. That method now shares the one-shot inventory-copy scatter used by ordinary death; it does not start ordinary game-over/Hub travel. Original items remain intact, copies cannot be picked up, and disabled-player destruction cleans them up. The old omission came from scatter existing only in the bypassed normal death coroutine.

- The user's `TutorialBoss_Scene` refers to the existing [DarkLord_Tutorial.unity](../../../Assets/_Project/Scenes/DarkLord_Tutorial.unity); no scene rename, route catalog or Build Settings change was needed. Its original first/second Ink assets, NPC illustration, defeat-screen text/settings and `ProtoTypeHub` return remain authored as before.
- [TutorialBossEncounterSequence.cs](../../../Assets/_Project/Runtime/Features/Tutorial/TutorialBossEncounterSequence.cs) uses `combatBoss`/`combatPlayerHealth` to opt into actual combat after the opening dialogue. It restores the player camera, HUD, controls and targetability immediately and calls `BeginCombatEncounter`. The intro timer pause is released for combat and reacquired on defeat. `ShouldMaintainPlayerLock` excludes the fighting and victory-handoff states so LateUpdate cannot reacquire cinematic locks during gameplay.
- The scene restores DemonKing's combat/physics/attributes/hurtbox/motion/status/pattern components from `LeeJunmo_Boss_DemonKing`. `startCombatOnStart` is false. Normal `BossEncounterDirector`/`BossDialogueRunner` are absent to prevent competing intro dialogue. The existing animator remains disabled for the opening tableau and is enabled at combat start. Scripted `TutorialBossLaserPresentation` is disabled/unreferenced, and its fake HP view is hidden/unreferenced. Ordinary boss attack patterns retain their normal behavior.
- Before damage is allowed, the sequence temporarily suspends that player's `PlayerDeathReturnToHub2D` and subscribes to real HP changes. At zero HP it fixes the loss outcome once, cancels boss-owned combat/patterns, stops movement and reacquires cinematic protection. It applies the existing player death state through `PrepareForScriptedDeathPresentation`, invokes collapse once and waits for `PlayerDeathPresentation2D.Play` before using the original second dialogue and unchanged `ShowFakeGameOver` request, including the Hub intro completion marker and `EndRunOnReturn=false`. The scripted laser/hit beat is not invoked; collapse now marks actual combat defeat. A defeated player is not re-enabled into the normal death route at zero HP, which would start a second game over; that player is scene-owned and removed during travel. Live-player cancellation/victory restores the previous enabled state.
- Boss `DeathStarted` fixes victory once and hands control to the restored `BossDeathPresentation` + `BossDefeatEndingSequence`. The latter references the existing victory Ink and an explicitly wired scene `EndingOutroPlayer`/EndingOutroView prefab using the existing outro sequence. Victory does not run the defeat dialogue or request the tutorial defeat screen. The first terminal outcome wins if both notifications arrive close together. After a loss only, a scoped CombatIncomingDamageModifiers registration rejects further damage to this boss, preventing lingering player attacks from launching its separate victory ending. Combat damage remains unchanged before defeat; victory/cancel/disable disposes the registration.
- Disable/cancel stops combat, unsubscribes HP/boss events and releases sequence-owned input/camera/HUD/timer state. Cinematic locks use existing owner tokens, so the boss-owned victory ending can retain its own protection after this sequence releases its tokens.
- The tutorial marks the boss FSM intro finished before Start, since it owns opening dialogue. The enabled combat Animator uses the DemonKing controller, not the old tableau Witch controller. The scene standalone EventSystem is inactive; GlobalUIRoot supplies the active one.
- Playable tutorial HUD hiding uses DialoguePlayback suppression ownership, acquired before dialogue and released with a 0.35-second fade for combat. Do not deactivate HUD roots before dialogue captures their state. Combat handoff also restores the tutorial-owned None interaction state if Editor scene normalization previously force-released protection.
- `DarkLordTutorialAuthoringTool` recognizes combat authoring during validation and prevents the old Apply menu from stripping the restored runtime components. Its laser-only setup remains a legacy mode for scenes without `combatBoss`; do not remove the combat reference to run that setup on this scene.
- Verification entry points: [TutorialBossCombatRegression.cs](../../../Tools/Validation/TutorialBossCombatRegression.cs) covers real HP subscription, input ownership, mutually exclusive outcomes and cleanup in an isolated Editor fixture. [TutorialBossSceneValidation.py](../../../Tools/Validation/TutorialBossSceneValidation.py) checks restored wiring, original dialogue/defeat configuration, victory outro, root reachability and portal destination. Full-scene combat, ending visuals and Hub return still need Play Mode acceptance. This scene-specific flow is not proposed for Architecture/Contracts promotion.

### Shared and legacy tutorial components

- WeaponSkillHUD2D and SwapWeaponSkillHUD2D project `PlayerCombatInput2D.IsWeaponInputBlocked`: the tutorial prerequisite hides both skill HUDs and restores them from current ability state on unlock. UI does not own or release the block; existing no-player/no-inactive-weapon visibility rules remain.

- TutorialCorridor's gate-facing exit door (door lock fileID `1767554932`, door at local y=27) enables `RoomDoorMonsterKillLock.holdAfterCombatUntilReleased`. After combat it stays closed until `TutorialChestOpenedTrigger` forwards the first successful chest UI open through `TutorialSceneSequenceDirector.onChestOpened` to `ReleaseAfterCombatHold`. Loot collection is not required. The entrance door and other room locks retain the default combat-clear behavior. Release is runtime state for this scene instance and does not bypass remaining monsters.
- Hub first-entry hides the upper frame with `DialoguePlayback.SetUpperPanelHiddenForCameraDialogue` before all focus-step dialogues and keeps it hidden until the camera returns to the NPC for final dialogue. The initial letterbox ends before these dialogues and never returns. Cancellation clears hiding; dialogue segments still own their normal opening/closing flow.
- TutorialCorridor enables `TutorialCombatIntroSequence.blockWeaponInputUntilTutorialComplete`. On player registration the scene sequence acquires an owner-scoped weapon-input block on the existing PlayerCombatInput2D. Only basic attacks and weapon skills are withheld; movement and dash remain available before the intro starts. Successful combat tutorial completion releases the block before onGameplayReleased. Cancellation alone keeps the prerequisite locked; player unregistration or component disable releases ownership, and re-enable before completion reacquires it. Cinematic protection can temporarily disable/re-enable the input component without losing this block.

- `TutorialInfoPanel` is the scene/prefab-authored UI driver for a tutorial explanation panel, including optional shared hold-button progress projection. The concrete implementation lives under `Assets/_Project/Runtime/UI/Tutorial` and implements Gameplay `ITutorialInfoPanel`.
- `TutorialInfoTrigger` opens a panel through `ITutorialInfoPanel` from a 2D trigger or from public methods such as `Fire`, `FireNow`, and `FireAfterDelay`. A `Collider2D` is needed only for trigger-entry activation, not for direct UnityEvent/code calls.
- `TutorialProgressStore` reads and writes tutorial completion through `GameDataManager`.
- `HubIntroProgressGate` centralizes the post-DarkLord Hub intro ids: `darklord_tutorial_forced_defeat_completed` and `hub_intro_after_darklord_seen`. It also owns the editor-only bypass check used by both the Hub fall presentation and the Hub intro sequence.
- `TutorialSaveData` stores completed tutorial ids in `GameData.tutorialData.completedTutorialIds`.
- `Assets/Sprites/UI/Tutorial/TutorialInfoWindow.png` and `TutorialInfoTitleRibbon.png` are the imported tutorial panel/title sprite assets.
- `TutorialInfoPage` is the authored page data shape for repeated tutorial layouts: page title, body text, and content image.
- `TutorialSceneSequenceDirector` is a scene-authored event coordinator for tutorial scene beats such as scene start, trigger entry, combat start, monster clear, chest open, portal entry, door open/close, player control lock, and player targetability blocking.
- `TutorialPlayerAutoMove` moves the authored player transform to a target point for tutorial presentation. It prefers existing `ExternalMovementController2D` / `MovementMotor2D` cleanup when present and falls back to Rigidbody2D or Transform movement.
- `TutorialBossEncounterSequence` coordinates the tutorial-only boss encounter flow: wait for and track player registration, hide normal runtime HUD roots, apply an authored initial player aim direction before locking player presentation, hold transition/player control/targetability locks, wait for scene transition fade to finish, briefly frame the player, boss camera focus, optional boss sprite scale, first dialogue, letterboxed scripted lasers, presentation HP HUD visibility, player hit feedback, immediate presentation death when tutorial HP reaches zero, second dialogue, player camera return without a second letterbox, and fake game-over presentation.
- `HubIntroAfterDarkLordSequence` is a Hub scene-authored continuation after the DarkLord forced-loss flow. It gates on the shared completion/seen ids, hides `GameplayHUDCanvas`, waits for the Hub fall presentation, locks player control, shows letterbox bars only for the opening NPC SpeechBubble beat, pre-sizes that opening SpeechBubble before typing, removes the bars before Dialogue, focuses the existing gameplay camera on the NPC and three authored Hub points, plays Dialogue segments, restores all temporary state, and marks the Hub intro seen.
- In `ProtoTypeHub`, `HubIntro_NpcFocusTarget` is a child of the speaking `Npc`, with local offset `(0, 1.27, 0)` (2026-09-18 correction). Opening and final NPC focus both reference this same anchor, so moving the NPC also moves the camera framing. The junk/training/gate focus points remain separately authored under the sequence root.
- `TutorialBossLaserPresentation` plays authored Demon King laser steps without connecting to real boss AI or damage. Each step can reduce only the presentation HP view.
- `TutorialPresentationHpView` projects tutorial-only HP through authored text, simple active roots, or `HeartTokenUI` slots. It does not read or write player `AttributeSet` or death state.
- `DarkLordTutorialAuthoringTool` is an Editor-only scene authoring menu for `DarkLord_Tutorial`. It creates the tutorial boss presentation root, focus targets, an `InitialAimTarget` marker, laser origins, a 3-slot presentation HP view using the existing `GlobalUIRoot` heart HUD layout/style, sequence/laser components, dialogue references when compiled Ink JSON exists, removes tutorial-unneeded real-boss runtime components from the scene `DemonKing` instance, and validates that conflicting real boss paths are absent.
- `HubIntroAfterDarkLordAuthoringTool` is an Editor-only scene authoring menu for `ProtoTypeHub`. It creates the Hub intro root and NPC/junk/training dummy/gate focus targets, adds/configures `HubIntroAfterDarkLordSequence`, configures the `PlayerSpawner.playerPrefab` `PlayerHubSpawnPresentation2D` gate/auto-wake defaults, can wire `MSUpgradeNpc` plus temporary Hub intro Ink JSON assets and their knot start paths for flow testing, and validates the remaining NPCData/Ink/scene reference wiring.
- `TutorialDefaultWeaponBootstrap` is a scene-authored tutorial loadout correction script. It forces the current player `WeaponInventory2D` to hold and equip the configured default weapon, optionally clearing other weapon slots.
- `TutorialScenePortal` is a tutorial-only direct scene portal for fixed authored jumps such as `TutorialCorridor` to `DarkLord_Tutorial`. It uses `SceneTransitionCoordinator`, prepares a minimal pending transition context, captures the current player runtime state before load, and does not create or consume a `PortalRouteManager` run plan.
- `TutorialDoorClosedTrigger` is a tutorial bridge that listens to `DoorObject.ClosedPresentationCompleted` and invokes an authored UnityEvent after a target door's close presentation completes. It can optionally require an assigned `RoomDoorMonsterKillLock` to be in an active encounter with remaining monsters.
- `TutorialCombatIntroSequence` is a scene-authored combat tutorial intro coordinator for the door-closed beat. It locks player controls, blocks player targetability, plays the cinematic letterbox, focuses the gameplay camera on an authored monster/chest target, fires the attack/skill `TutorialInfoTrigger`, waits for the panel to close, returns the camera to the player, plays letterbox-out, then releases gameplay.
- `TutorialChestOpenedTrigger` is a tutorial bridge that listens to a target `TreasureChest` successful UI-open event and invokes an authored UnityEvent for the next tutorial step.
- `TutorialPlayerHealthAutoRecover` is a scene-authored tutorial safety component that detects real player HP loss, keeps lethal hits at a minimum survival HP, then restores the current player HP attribute to max after the configured delay. It can temporarily disable `PlayerDeathReturnToHub2D` while active so a tutorial scene cannot route to game over before the recovery lands.

## Ownership And Lifecycle

- UI hierarchy, canvas, TMP text, Images, and sprite assignment should be authored in a scene or prefab, then referenced by `TutorialInfoPanel`.
- `TutorialInfoPanel.hideOnPlayStart` defaults to enabled so an authored active UI can be reviewed in Scene view but is hidden automatically when Play Mode starts.
- `TutorialInfoPanel` blocks normal gameplay input while open through `GameFlowInputBlocker` when `blockGameFlowWhileOpen` is enabled.
- `TutorialInfoPanel` owns page index state. Prev/Next controls are scene-authored buttons whose roots stay visible while multiple pages exist; invalid directions are disabled through `Button.interactable` instead of hidden. Optional `pageNumberText` displays the current page as `1/2`. A/D page keys use `InputKeyCompatibility` and do not require runtime-created UI. Optional page key guide roots and KeyGlyph Images stay visible while multiple pages exist, with invalid directions projected through disabled color/tint.
- The hold-confirm button is only active/usable on the final page. Before the final page, `TutorialInfoPanel` resets hold progress, disables the authored `HoldActionButton`, and deactivates the authored hold-button root.
- Panel progress uses an authored `HoldActionButton` when assigned. In that mode the button's own hold time, input settings, and reset policy are the source of truth.
- If no `HoldActionButton` is assigned, `TutorialInfoPanel` falls back to the existing `InputBindingService` `DialogueAdvance` binding and uses request/panel hold seconds.
- `TutorialInfoTrigger` can be placed on a trigger collider for player entry, or fired directly from code/UnityEvent for specific timing. Direct-call triggers do not need a `Collider2D`. Its `infoPanel` field is a `MonoBehaviour` reference that must point to a component implementing `ITutorialInfoPanel`.
- `TutorialSceneSequenceDirector` can optionally wait for an active `SceneTransitionCoordinator` / `SceneFadeTransitionService` transition to finish before firing scene-start events. By default it starts its scene-start delay as soon as the tutorial scene is loaded, which is the intended path for post-title FadeOut startup beats.
- Completion persistence is opt-in per request/trigger and should use stable non-empty tutorial ids.
- Open presentation is owned by the panel: an optional authored `dimPanel` `CanvasGroup` fades in while an optional authored `tutorialPanel` root moves up from `hiddenPanelOffset` using an overshoot ease. Close plays the reverse and deactivates the root after the presentation finishes.
- Tutorial scene flow scripts do not create scene content. Doors, HP UI, laser origins, boss visuals, camera focus targets, collapse effects, and scene portals remain authored in Unity and are connected through serialized references or UnityEvents.
- `TutorialBossEncounterSequence` may use `CameraPresentationDirector` when a boss-camera setup exists, but its default tutorial path focuses the existing gameplay camera through `CameraBootstrap` so a non-boss tutorial scene can still frame a placed boss sprite. Tutorial camera focus/return waits treat serialized wait seconds as minimum holds, then wait for the camera to settle on the target.
- `TutorialBossEncounterSequence` waits for `PlayerRuntimeRegistry`/`PlayerInteractor2D`, subscribes to player register/unregister events, and holds a `SceneFadeTransitionService` player-unlock blocker before acquiring player protection. Serialized validation can confirm the lock fields, but only runtime player resolution proves the lock was actually acquired.
- `TutorialBossEncounterSequence.applyInitialAimOnSequenceStart` is applied immediately before `PlayerCinematicProtection` disables `PlayerAim2D` and before `WeaponPresentationRig2D` acquires its cinematic presentation lock. When `initialAimTarget` is assigned, the sequence aims from the spawned player toward that marker; otherwise it uses `fallbackInitialAimDirection`.
- `TutorialBossEncounterSequence.keepPlayerLockedAfterSequence` keeps the defeated player locked through the defeat screen; it does not apply to the combat or victory window. Completion cleanup releases camera, letterbox, UI input blocker, and timer pause, but keeps player protection/targetability and the transition unlock blocker until the component disables or the scene unloads.
- `TutorialBossEncounterSequence` keeps letterbox bars off while Ink dialogue is visible. It turns the bars on only for non-dialogue presentation beats, so Dialogue UI, speaker name, and choices are not covered.
- In `DarkLord_Tutorial`, the authoring tool disables the `CameraPresentationDirector` path and uses `BossFocusTarget` / `PlayerFocusTarget` through the gameplay camera. This keeps the tutorial sequence independent from real boss `BossCam` authoring.
- Fake tutorial game-over calls `GameOverPresentationPlayback.TryShow(...)` with `GameOverPresentationRequest.EndRunOnReturn = false`, tutorial-only `ReturnButtonLabel = "추락"`, message override `처치자 마왕`, location override `마왕의 알현실`, and hidden remaining-time text; real HP, real death components, normal game-over text, and run end state are not changed by the tutorial sequence.
- Before fake tutorial game-over opens, `TutorialBossEncounterSequence` marks `darklord_tutorial_forced_defeat_completed` so Hub-side introduction gates can distinguish this route from ordinary first Hub entry.
- `PlayerHubSpawnPresentation2D` can keep its default `AnyHubSpawn` behavior, or be set to `AfterDarkLordTutorialUntilSeen` for the post-DarkLord Hub route. In that mode it uses `HubIntroProgressGate` and can wake the player automatically through `autoWakeWithoutInput` / `autoWakeDelaySeconds`.
- `HubIntroAfterDarkLordSequence` hides only the existing `GameplayHUDCanvas` and restores its previous enabled state on completion/cancel. It does not author or instantiate gameplay HUD UI.
- The Hub intro sequence waits until `PlayerHubSpawnPresentation2D.IsPlaying` is false before acquiring `PlayerCinematicProtection`, because the fall presentation restores player input on wake.
- The Hub intro opening SpeechBubble uses the existing pre-sized speech-bubble path from RunSpecial NPC when `preSizeOpeningSpeechBubbleBeforeTyping` is enabled. This measures the full line, clamps text width, and grows the bubble before the typing tween starts.
- The Hub intro sequence removes cinematic letterbox bars after the opening NPC SpeechBubble and before starting any Ink Dialogue, so the Dialogue UI is not covered during the three Hub focus explanations or final NPC segment.
- Dialogue presentation options extend the existing Dialogue path without changing default callers. Hub focus steps use no portrait intro/outro; the final NPC segment can use a fast silhouette portrait intro.
- Dialogue opening previews the first playable Ink line without consuming it and passes that line's `face` tag to the portrait intro. Boss silhouette intros therefore use the first authored expression, such as `# face:Tutorial`, instead of always starting from `Normal`.
- `DarkLordTutorialAuthoringTool` creates a `TutorialPresentationHpCanvas` as a temporary presentation-only HP readout with three heart slots copied from the existing HUD token/sprite style and layout. It deactivates the normal Gameplay/Boss HUD roots for `DarkLord_Tutorial`; the tutorial HP canvas is hidden during Script 1, shown for the laser letterbox sequence, hidden before Script 2, and is not connected to actual player `AttributeSet` HP or death behavior.
- When `DarkLord_Tutorial` unloads or the sequence is canceled with lock release, `TutorialBossEncounterSequence` restores a snapshot of the normal Gameplay/Boss HUD canvas roots and default HUD component root active states so the persisted `GlobalUIRoot` can show normal HUD again in Hub without forcing previously hidden elements on.
- Tutorial boss player locks use `PlayerCinematicProtection`, which now combines input behaviour disabling with the existing UI control block tag set and an explicit `State.Move.Dash.Blocked` tag. This is required because cached movement sources and GAS dash activation can outlive a simple input component disable.
- Tutorial-only default weapon enforcement is scene-local. `TutorialDefaultWeaponBootstrap` uses `WeaponInventory2D.TrySetWeaponSlot(...)` and `Equip(...)` so existing weapon removal, stat binding, presentation binding, ability ownership, and equipped-change events remain owned by the weapon inventory path.
- `TutorialDefaultWeaponBootstrap.applyOnStart` waits authored startup frames before applying so player spawn and pending runtime restore have a chance to finish first. Use `ApplyNow()` from a scene director/UnityEvent when the tutorial needs an explicit timing point.
- A tutorial-only weapon that must survive scene travel still has to be registered in the active `ItemDatabase.allWeapons`, because player runtime restore resolves captured weapon ids through `ItemManager`. It does not need to be in `defaultUnlockedWeapons` unless normal unlock/drop access is intended.
- Tutorial fixed scene travel should not use `ScenePortal` unless the travel is intentionally part of the normal run route plan. `TutorialScenePortal` owns direct scene load interaction, visual highlight projection, current player runtime-state capture, and a minimal pending transition context for the destination `PlayerSpawner`/restore bootstrapper.
- Combat tutorial prompts that must wait until the room door visually closes should use `TutorialDoorClosedTrigger` instead of guessing with a trigger delay. `DoorObject` owns only the generic close-completion signal; tutorial panel or sequence calls remain wired through the bridge UnityEvent.
- Combat tutorial intro presentation should use `TutorialCombatIntroSequence` when the authored flow needs camera focus before the attack/skill prompt. The sequence owns only temporary control/targetability/camera/letterbox ownership and releases gameplay after camera return and letterbox-out finish before raising its gameplay-released event.
- Combat tutorial chest continuation should use `TutorialSceneSequenceDirector.OnMonstersCleared` for the post-combat chest-open prompt, then `TutorialChestOpenedTrigger.OnChestOpened` for the step that should run after the chest UI opens.
- Tutorial scene HP auto-recovery should stay scene-local through `TutorialPlayerHealthAutoRecover`; do not change shared player damage, death, or combat rules for this tutorial exception.

## Extension Entry Points

- Add authored panel prefab/scene wiring by assigning a `TutorialInfoPanel` component to the `MonoBehaviour` fields that expect `ITutorialInfoPanel`.
- Leave `hideOnPlayStart` enabled for scene-authored tutorial UI so test runs start with the panel hidden. Disable it only for a focused UI preview scene that intentionally starts with the panel visible.
- Add tutorial content by filling `TutorialInfoTrigger.pages`. Page data is the only tutorial content path; empty pages open a blank panel and should be treated as an authoring miss.
- Assign `previousPageButton` / `nextPageButton` and, when the clickable visual root differs from the Button GameObject, assign `previousPageRoot` / `nextPageRoot` so the controls can stay visible while their `Button.interactable` state changes. Assign `pageNumberText` when the authored layout should show `current/total`.
- Assign `pageKeyGuideRoot`, `previousPageKeyGuideRoot`, `nextPageKeyGuideRoot`, and optional `previousPageKeyGlyphImage` / `nextPageKeyGlyphImage` when the authored layout should show A/D page navigation hints. If the Image references are left empty, `TutorialInfoPanel` resolves an `Image` under each guide root. The individual A/D guide roots stay visible while multiple pages exist, and invalid directions use `disabledPageKeyGlyphColor`.
- Assign `dimPanel` and `tutorialPanel` when open/close presentation is desired. Leave them empty for immediate legacy-style show/hide.
- Add a hold-confirm button by authoring a `HoldFillButtonView` and optional `HoldActionButton`, then assigning them to `TutorialInfoPanel`. Assign `advanceHoldButtonRoot` when the visual root differs from the `HoldActionButton` GameObject.
- Set per-layout hold timing on the authored `HoldActionButton` prefab/variant when a button is assigned; `TutorialInfoTrigger.holdSeconds` remains fallback-only for buttonless panels.
- Add tutorial timing by calling `TutorialInfoTrigger.FireNow` or `FireAfterDelay`.
- For event-driven tutorial timing such as door-close prompts, place `TutorialInfoTrigger` on any convenient scene object without a collider and call `FireNow()` from the owning sequence or UnityEvent.
- Add one-shot guidance by setting `usePersistentCompletion` and `markCompletedOnClose` with a stable `tutorialId`.
- Add scene flow by placing `TutorialSceneSequenceDirector` in the tutorial scene and wiring its UnityEvents from trigger colliders, chest open events, monster clear locks, portal entry, and authored door controls. For intro-to-tutorial startup, enable `playOnStart`, leave `waitForSceneTransitionBeforeStart` disabled, set `startDelaySeconds`, then wire `onSceneStarted` to the first `TutorialPlayerAutoMove.MoveToTarget`. Enable `waitForSceneTransitionBeforeStart` only when the scene-start event should wait until fade-in and the full transition session are complete.
- Add a start walk-in by wiring `TutorialPlayerAutoMove.MoveToTarget` from the scene director or a trigger event. Assign the player, target point, and existing movement components when available.
- Add tutorial boss encounter by placing `TutorialBossEncounterSequence`, assigning a tutorial-only `NPCData`, first/second Ink TextAssets, optional `TutorialBossLaserPresentation`, optional `TutorialPresentationHpView`, boss focus target, boss visual root, and fake game-over return scene.
- Add the post-DarkLord Hub introduction by setting the Hub player prefab/instance `PlayerHubSpawnPresentation2D.playCondition` to `AfterDarkLordTutorialUntilSeen`, enabling `autoWakeWithoutInput` if the player should rise without input, and placing `HubIntroAfterDarkLordSequence` in the Hub scene.
- For `HubIntroAfterDarkLordSequence`, assign the NPC SpeechBubble, NPC focus target, narrator `NPCData`, three focus steps for junk/training dummy/gate, the final NPC focus/dialogue, and matching completion/seen ids. Leave runtime UI hierarchy creation out of the scene; use existing Dialogue, SpeechBubble, and letterbox patterns.
- Use the opening SpeechBubble layout fields when the intro bark is long: leave `preSizeOpeningSpeechBubbleBeforeTyping` enabled, keep min/max text width near the RunSpecial defaults of `160`/`360`, and tune only if the authored bubble art or camera framing requires a different width.
- For `ProtoTypeHub`, prefer running `Tools/Tutorial/Hub Intro After DarkLord/Apply Default Authoring To Active Scene`, then run `Tools/Tutorial/Hub Intro After DarkLord/Create Or Wire Draft Dialogue Assets` if final NPCData/Ink content is not ready, then run `Validate Active Scene`. The tool selects the generated root and marks the scene/player prefab dirty when it changes them; save only after reviewing focus target positions and assigned references.
- For `DarkLord_Tutorial`, prefer running `Tools/Tutorial/DarkLord Tutorial/Apply Default Authoring To Active Scene` with that scene active, then run `Validate Active Scene` after Unity finishes importing/compiling the tutorial Ink files. Save the scene only after reviewing the generated root, references, and `InitialAimTarget` position.
- Add laser beats by filling `TutorialBossLaserPresentation.steps` with origin transforms, direction, length, width, warning/attack timing, optional opposite ray, and an authored `DemonKingEgoLaserVfx` prefab.
- Use `TutorialBossEncounterSequence` UnityEvents for authored hit, collapse, sound, VFX, and animation hooks instead of connecting to real death/damage systems.
- Add tutorial-only default loadout by placing `TutorialDefaultWeaponBootstrap`, assigning the intended `defaultWeapon`, and leaving `clearOtherWeaponSlots` enabled when the scene must start with only that weapon.
- Add a tutorial-only fixed portal by placing an interactable object with a trigger `Collider2D`, adding `TutorialScenePortal`, and setting `targetSceneName` to the destination scene, for example `DarkLord_Tutorial`. Leave `resetPlayerRuntimeStateOnTravel` disabled when the destination should keep the tutorial loadout. The destination scene must be in BuildSettings.
- Add a simple combat-room close prompt by placing `TutorialDoorClosedTrigger` on or near the target door, assigning the `DoorObject`, optionally assigning the room's `RoomDoorMonsterKillLock`, and wiring `OnDoorClosed` to `TutorialInfoTrigger.FireNow()` or a `TutorialSceneSequenceDirector` method.
- Add the attack/skill combat intro by placing `TutorialCombatIntroSequence`, assigning the same `TutorialDoorClosedTrigger` or wiring the trigger's `OnDoorClosed` to `BeginSequence`, assigning `focusTarget` to an authored marker near the monster/chest composition, assigning the attack/skill `TutorialInfoTrigger`, and assigning the shared `TutorialInfoPanel` component to the sequence `infoPanel` field when the sequence should wait until the panel closes. Leave `useLetterbox` enabled for the cinematic bars. Leave `fadeGlobalUiDuringLetterbox` disabled unless the target tutorial panel is on a layer that will stay visible.
- Use `TutorialCombatIntroSequence.OnGameplayReleased` for authored events that should happen only after the camera returns to the player, letterbox-out completes, and control/targetability are restored.
- Add post-combat chest guidance by assigning `TutorialSceneSequenceDirector.chestMonsterKillLock` or `roomDoorMonsterKillLock`, starting its monster-clear wait from the combat-start/release beat, and wiring `OnMonstersCleared` to the chest-open `TutorialInfoTrigger.FireNow()`.
- Add chest-open continuation by placing `TutorialChestOpenedTrigger` on the target chest or a nearby scene object, assigning the target `TreasureChest`, and wiring `OnChestOpened` to `TutorialSceneSequenceDirector.NotifyChestOpened()` plus any next `TutorialInfoTrigger.FireNow()` call.
- Add tutorial HP safety by placing `TutorialPlayerHealthAutoRecover` in the tutorial scene and assigning the health `AttributeDefinition` and max-health `AttributeDefinition`. Player, `AttributeSet`, and `PlayerDeathReturnToHub2D` references may stay empty when the scene uses `PlayerRuntimeRegistry` spawning. Leave `suspendPlayerDeathReturn` enabled when the scene must never route to game over from combat damage.

## Known Pitfalls

- Empty tutorial ids are never recorded as completed.
- The Hub fall presentation and Hub intro sequence must use the same completion/seen ids for real tutorial continuation. For direct `ProtoTypeHub` Play, both presentations additionally consult Core `EditorDirectSceneStartContext` and skip together so development portal testing begins with an idle player; the marker clears on the next scene and does not suppress real Title/tutorial entry or later Hub returns.
- Missing Hub intro Dialogue TextAssets or narrator `NPCData` are authoring mistakes. The sequence logs missing narrator/service errors, but scene wiring and Play Mode review are still required to confirm every dialogue segment appears.
- `HubIntroAfterDarkLordAuthoringTool` can add/configure `PlayerHubSpawnPresentation2D` on `PlayerSpawner.playerPrefab`; this is a prefab-facing editor action, not a runtime mutation. Review the prefab dirty state before saving, especially if another scene also uses the same player prefab.
- `HubIntroAfterDarkLordAuthoringTool` wires the existing `MSUpgradeNpc.asset` as the temporary narrator and uses temporary Ink/JSON TextAssets under `Assets/LeeJunMo/Datas/Inks/HubIntroAfterDarkLord` to unblock flow validation. These assets are knot-based, so the tool also fills and validates the matching `dialogueStartPath` / `finalDialogueStartPath` values. These are placeholders for play-flow testing; replace their text before final content sign-off.
- `HubIntroAfterDarkLordAuthoringTool` does not create SpeechBubble prefabs or runtime UI hierarchy. Validation errors for missing `narratorNpcData`, `openingSpeechText`, focus step Ink JSON, and final Ink JSON can be resolved either by final Inspector wiring or by the draft dialogue asset menu.
- If the Hub intro opening SpeechBubble extends off-screen while typing, confirm `preSizeOpeningSpeechBubbleBeforeTyping` is enabled and `openingSpeechBubbleMaxTextWidth` is not set too high. The Dialogue segments are unrelated because they use the Dialogue UI, not the SpeechBubble layout path.
- The Hub intro sequence adds `PlayerCinematicProtection` at runtime only as the existing protection pattern when the player lacks it; it does not add a new manager or persistent object.
- Unity import/compile must refresh new script and sprite metadata before prefab wiring.
- `TutorialInfoTrigger` no longer requires a `Collider2D`. If a tutorial should fire from player entry, the scene object must still have an authored trigger `Collider2D`; otherwise only `Fire`, `FireNow`, or `FireAfterDelay` calls will open it.
- `TutorialInfoTrigger.infoPanel` and `TutorialCombatIntroSequence.infoPanel` are widened to `MonoBehaviour` for assembly separation; assigning a component that does not implement `ITutorialInfoPanel` will be ignored at runtime and should be treated as an authoring error.
- Do not expect `TutorialInfoTrigger.holdSeconds` to override an assigned `HoldActionButton`; change the button prefab/variant when a button-led tutorial needs a different hold duration.
- Page title text comes from `TutorialInfoPage.title`. Leave a page title empty only when the authored panel title should be blank for that page.
- Page key guide sprites are visual-only. Changing a KeyGlyph Image sprite manually can be overwritten from `InputGlyphDatabase`; change `previousPageKey` / `nextPageKey` for actual keyboard navigation.
- Prev/Next disabled visuals now depend on the authored `Button` transition/color settings. Key guide disabled visuals use `disabledPageKeyGlyphColor` and restore each Image's authored color when the direction becomes valid again.
- `tutorialPanel` should point to the actual TutorialPanel/content root, not the full-screen root that contains `DimPanel`; otherwise the whole overlay can move.
- `dimPanel` should point to the DimPanel child/sibling `CanvasGroup`, not the same root that owns `TutorialInfoPanel`. The runtime avoids deactivating the panel root if misassigned, but the fade authoring is clearer with a dedicated DimPanel object.
- Presentation HP must stay separate from actual player HP. Do not call player damage, death, or `AttributeSet` mutation from tutorial boss laser UnityEvents unless the tutorial design explicitly changes to real damage.
- Tutorial boss laser hits may call `PlayerHitFeedback2D`, and collapse may call `PlayerDeathPresentation2D`, but these are presentation-only calls. Do not use `PlayerDeathReturnToHub2D` or real HP mutation for the scripted failure unless the design explicitly changes.
- A passing `DarkLordTutorialAuthoringTool` validation does not prove the runtime player lock was acquired. Play Mode review must confirm `TutorialBossEncounterSequence` waited for player registration and that movement/dash/combat/aim input stays blocked.
- In legacy laser-only authoring, copied normal boss dialogue/combat components can race the sequence. In the current playable DarkLord scene, combat/FSM components are required; only the competing `BossDialogueRunner`/`BossEncounterDirector` are omitted. The legacy Apply menu is guarded and must not be used to strip playable combat.
- If `DarkLord_Tutorial` authoring was applied before the HUD-layout HP and permanent-lock defaults, rerun the Apply menu and save the scene after review.
- If tutorial HP should remain visible during letterbox, put it on a canvas/layer that is not included in `TutorialBossEncounterSequence.fadedLayers`, or disable `useCustomFadedLayers` only when default global UI fading is acceptable.
- If the boss focus camera does not move in `DarkLord_Tutorial`, rerun the DarkLord authoring Apply menu and validate that `useCameraPresentationDirector` is disabled. The tutorial should not depend on the real boss `BossCam` path.
- If entering `DarkLord_Tutorial` from `TutorialScene` shows normal HUD during fade-in, confirm `TutorialBossEncounterSequence.hideDefaultHudDuringSequence` is enabled and the sequence starts on scene load. Scene-only inactive HUD authoring does not affect an already-persisted `GlobalUIRoot`.
- If the player or equipped weapon faces the wrong way at `DarkLord_Tutorial` entry, move `__TutorialBossPresentation/InitialAimTarget` in Scene View and rerun validation. The marker controls the initial `PlayerAim2D` direction captured by the cinematic weapon presentation lock.
- If the fake HP UI is referenced but invisible, inspect `TutorialPresentationHpCanvas` first. Its GameObject must be active, its RectTransform should match the full-screen overlay authoring defaults, and its canvas sorting must be high enough to render above the cinematic letterbox. Visibility should be controlled by `TutorialPresentationHpView.visibilityGroup` alpha, not by disabling or stale canvas authoring.
- Tutorial boss Ink line text should not include visible speaker prefixes such as `마왕:`. Use `# speaker: 1005` for DarkLord lines and `# speaker: ???` for temporary hidden-speaker lines.
- Tutorial boss Ink that needs a non-`Normal` opening silhouette should put the intended expression on the first playable line with `# face:Tutorial` or `# face:1005:Tutorial`. The Dialogue preview does not advance the real Ink story, but Play Mode should confirm the sprite label exists in the assigned `SpriteLibraryAsset`.
- For TMP rich text inside Ink body text, avoid raw hex color tags such as `<color=#A855F7>` unless the generated JSON is verified. Ink can parse `#A855F7...` as an Ink tag and leave only `<color=` in the visible line. Prefer named colors such as `<color=purple>` for tutorial dialogue.
- The auto-created tutorial boss HP canvas is a temporary authoring convenience. Replace it with an authored scene/prefab UI later if the final tutorial HUD needs custom art or layout.
- New tutorial boss scripts add MonoBehaviours and serialized references, so Unity Editor import/compile and Inspector wiring are required before scene authoring can be considered complete.
- `DarkLordTutorialAuthoringTool` does not edit scene YAML directly and does not save the scene. The active scene must be `DarkLord_Tutorial`, Play Mode must be stopped, and the user must save after reviewing the dirty scene.
- The DarkLord tutorial Ink `.json` references are generated by Unity import. Until `TutorialDarkLordScript1.json` and `TutorialDarkLordScript2.json` exist, Apply/Validate will report missing dialogue TextAssets even though the `.ink` sources are present.
- Automated laser origins, HP canvas placement, and camera focus targets are defaults only. Scene View tuning is still required if the copied boss room layout or player spawn differs from the assumed positions.
- The system currently provides only the common frame and boss encounter support scripts; actual tutorial text, enemy/chest/gate placement, Ink content, and layout positions remain out of scope.
- `TutorialDefaultWeaponBootstrap` changes the live player inventory for the current runtime session. If a later transition should discard tutorial loadout state, that transition should reset or replace the run state deliberately instead of relying on this script to preserve the previous loadout.
- If a tutorial default weapon is captured but disappears after scene travel, confirm the weapon asset is present in `ItemDatabase.allWeapons`. `PlayerRuntimeResolverBridge.ResolveWeapon(...)` cannot restore a captured `weaponId` that the active item database cannot resolve.
- `TutorialScenePortal` bypasses the normal portal route pipeline but still captures current player runtime state directly. If a future tutorial portal intentionally must discard the current loadout, enable `resetPlayerRuntimeStateOnTravel` or reset the destination loadout deliberately.
- `TutorialDoorClosedTrigger` depends on `DoorObject` close-completion detection. Animator-driven doors complete when the animator reaches the closed `Idle` state, a non-looping close state finishes, or the fallback timeout is reached; an optional `OnCloseAnimationComplete` animation event can make authored timing explicit.
- `TutorialCombatIntroSequence` swaps the existing gameplay camera's Follow/LookAt targets. Manual validation should confirm the authored focus marker frames both monster and chest at the selected orthographic size, and that camera return damping feels acceptable.
- `TutorialCombatIntroSequence.fadeGlobalUiDuringLetterbox` is disabled by default so the attack/skill tutorial panel is not faded out by the cinematic overlay. If enabled, review `fadedLayers` carefully because the default `CinematicLetterboxOverlay` layer set includes prompt/dialogue-style UI.
- `TutorialCombatIntroSequence` releases targetability only after returning to the player. If another scene event also acquires `PlayerTargetabilityBlocker`, enemies may still ignore the player until that owner releases its token.
- `TutorialChestOpenedTrigger` fires from `TreasureChest.FirstOpenedUi` by default, which is raised after `ChestUIManager.OpenChest(...)` succeeds. It will not fire if the chest interaction request starts but the chest UI fails to open.
- If a tutorial should respond every time an already-open chest UI is reopened, disable `firstOpenOnly`; otherwise leave `triggerOnce` and `firstOpenOnly` enabled for one-shot tutorial continuation.
- `TutorialPlayerHealthAutoRecover` changes live player HP state, can temporarily disable `PlayerDeathReturnToHub2D`, and lets HP stay low until its recovery delay completes. Keep it out of non-tutorial scenes and verify the health/max-health attribute references in the Inspector.

## Promotion Candidate

This can become a future contract if tutorial authoring rules become shared across multiple scenes.


### Lower dialogue frame across Hub camera steps

Hub enables DialoguePlayback.SetLowerPanelRetainedBetweenDialogues across focus-step dialogue sessions. DialogueView skips lower-frame exit and re-entry while retaining the authored panel, so the previous line stays visible during camera travel. Final dialogue consumes that retained frame and disables retention before its own normal closing. Cleanup releases any retained frame between sessions. GrandHall already uses one live dialogue with blocking camera cues, so its lower frame remains visible without this option.

### Gunner retreat animation ownership correction (2026-09-18)
- The gunner Animator is authored on a child, and Mob.UpdateAnimation owns its isMoving parameter using the existing motor. PrototypeTutorialUpgrade does not look up or drive a root Animator. An exception in that former lookup prevented retreat completion and therefore blocked held-hit progress. The regression fixture now mirrors the child-only hierarchy and checks nine held hits after retreat.

### Tutorial combat HUD and projectile passage (2026-09-18)
- QuestHudView keeps missions visible during combat in PrototypeTutorialUpgradeScene, TutorialCorridor, TutorialScene and DarkLord_Tutorial; ordinary combat retains its hide animation. This does not override cinematic HUD suppression.
- Prototype bullets remain active/unconsumed when crossing an invulnerable player. The general AttackBase/CombatHitPayloadApplier path already rejects invulnerable damage without invoking hit destruction. DemonKingProjectile2D now also destroys only after successful payload application, allowing passage during dash invulnerability. Lifetime and ordinary collision behavior remain.

### Tilemap authoring structure verification (2026-09-18)
- ConfigurePrototypeTutorialTilemapStructure.py is a guarded one-time structure migration, preserving serialized Tilemap blocks and transforms. Source-map composite cached paths are cleared instead of copied; Unity builds geometry for the tutorial tiles.
- PrototypeTutorialTilemapStructureValidation.py checks layers, sorting, wall components, disabled blockout colliders and optional pre-change snapshot preservation. PrototypeTutorialTilemapNativeValidation.cs verifies the extracted authored grid in isolated Unity: collision generation and paint/erase refresh.
- PrototypeTutorialLayoutValidation.py remains a historical blockout rectangle check; it does not certify current painted sprite collision. PrototypeTutorialEncounterValidation.py retains the initial shipping-palette assumption and is not the acceptance check for the user-repainted tilemap. Use the new structure/native checks for the current map. Full scene playthrough, AI navigation and gate-bypass review remain manual.

### Chest world guidance (2026-09-18)
- PrototypeChestNavigation also owns scene-wired worldChest/worldArrow guidance before opening the reward screen. Stage 4 acquires ChestInteractable.SetGuidanceHighlight with this owner, using the existing white OutlineMaterial. A scene-authored HubWeaponGuidanceArrow prefab instance follows GetPromptAnchor plus 1.2 units with the same .2-unit/.6-second unscaled bounce as HubWeaponDepartureGuide.
- Opening the matching chest UI hides world guidance; closing before completion restores it. Completing selection, leaving stage 4 or disabling the navigation releases its outline ownership and hides its authored arrow. The existing first-slot spotlight, input restriction and instant confirm-color pulse remain unchanged. Shared Hub guidance, chest prefab and ItemDetailPanel are unchanged.
- PrototypeTutorialWorldGuideValidation.py checks scene actions/stages, BlackOutline materials, removed background, arrow/chest references and tilemap preservation. PrototypeTutorialWorldGuideRegression.cs verifies stage-bound presentation and highlight cleanup in isolated Unity. Rendered full-scene layout/playthrough remains manual.

### Floor guide readability tuning (2026-09-18)
- All seven guides use Bold instructions at 40 and fallback key labels at 32. Instruction rectangles use left alignment/left pivot, starting 0.25 world units after the normalized glyph's right edge, so shorter text no longer drifts away from its icon.
- First-room movement/dash guide roots are (8,-4.2) and (8,-2.5), with matching BlackOutline/background-free styling. Movement remains stage 0; dash remains persistent. StylePrototypeTutorialWorldGuides.py applies these authored settings without changing tile or gameplay data.

- MovementFloorGuide authors four bound glyphs in W-over-A/S/D layout (0.55-unit normalized size). The primary MoveUp controller owns stage visibility; three additional controllers on the always-active root only refresh MoveLeft/MoveDown/MoveRight bindings. All glyphs/fallbacks are children of the shared Presentation root. Its caption starts at x=-1.85; other guide spacing remains unchanged. Dash uses only its mapped key glyph plus the short dash instruction.


### Prototype blockout retirement and boss portal door (2026-09-18)
- Movement/dash goal pads and unused weapon-placement pads retain transforms but have disabled SpriteRenderers. Old floor/wall rendering remains disabled. All four legacy MissionGate objects and colliders are inactive; the scene's gates array is empty, so stage refresh cannot restore them. Functional bullets and bound floor instructions remain visible.
- One scene-authored Door prefab instance, TutorialBossPortalDoor, sits at (0,45.6) with scale (2,2,1), spanning the existing four-unit portal-room entrance. It uses Locked DoorType and isPermanent=false; no shared prefab or painted tiles changed.
- PrototypeTutorialUpgrade subscribes to portalDoorChest.FirstOpenedUi while enabled and opens bossPortalDoor through DoorObject.ForceOpen(save:false). Door visuals/collision remain owned by DoorObject. Opening does not advance the item-selection mission. Re-enable restores door state from the chest's IsOpened; disable releases the event subscription. Chest completion still advances stage separately through PrototypeChestNavigation.

- Gunner fires exactly one shot after crossing bulletFireStart at (0,.5), within .75 horizontal units of the corridor center. This is roughly one unit after the painted narrow entry at y=-.5. At that point PrototypeTutorialUpgrade closes its authored dodgeEntranceDoor through DoorObject. The shot latch prevents recycling after impact/despawn and resets on enable, which also opens the entrance immediately. TutorialDodgeEntranceDoor uses the existing Door prefab at (0,-.5), unit scale, Locked and nonpersistent; collision/animation remain owned by DoorObject. The movement-stage trigger remains separate.


### Exact-boundary grid normalization (2026-09-18)
- TutorialTileGrid Transform scale is now (1,1,1), with Grid cell size (.5,.5,0), as explicitly accepted to retain every existing half-unit boundary. This is not a conversion to a uniformly one-unit painting lattice.
- Identical 2x2 Ground cells merge to one tile: Ground 2699 -> 746; Wall remains 768. Retained cells use half-size tile matrices; merged cells use full-size matrices with (.25,.25) offset. Each original cell still maps to the same tile/sprite/color identity. Different artwork/color cells never merge. Existing entity/door/guide transforms are unchanged.
- Wall tile shapes remain the same size/position, with composite local vertex/offset tolerances halved to retain world tolerances. Tilemap structure validation retains its old raw-paint equality check only for the old grid scale; use TutorialGridNormalizationSceneValidation plus TutorialGridNormalizationValidation for normalized coverage and native collision checks.
- Editing caveat: this map uses per-cell transform matrices, so painting a fresh tile with default asset transforms is not equivalent to copying a retained half-size boundary tile. Preserve/copy the appropriate tile transform when touching boundaries. A future uniformly one-unit layout would require accepting boundary changes.
- Recovery audit found identical current tile placement/types in all available task snapshots and the binary Unity editor backup. Git versions predate the tilemap. No separate lost blue-carpet layout was recoverable from these sources; no inferred repaint was performed.
### Unit-cell tutorial map rebuild (2026-09-18)

The user's later request supersedes the half-unit grid normalization described below. TutorialTileGrid now has Cell Size (1,1,0), unit Grid/Ground/Wall Transform scales and identity tile matrices. Ground contains 662 full-unit tiles and Wall 218. Rooms and connections are rebuilt on integer cells: start room x[3,13), y[-8,0); side entry x[1,3), y[-3,-1); two-unit dodge lane x[-1,1), y[-3,20); training room x[-10,10), y[20,38); boss approach x[-2,2), y[38,46); boss room x[-7,7), y[46,56). A one-cell wall border encloses the connected floor union. Dominant existing artwork is reused per cell, with nearest existing artwork at adjusted boundaries.

Door, player, monster, objective, guide and portal objects retain their authored transforms and references. Half-unit outlines are intentionally replaced, so the earlier exact-wall-vertex preservation claim describes only the previous migration. DoorObject continues to own the two dynamic doorway obstacles; the static Wall Tilemap retains its existing merged collider setup. Tools/Validation/TutorialUnitGridValidation.py checks connectivity, unit matrices, corridor width and unchanged non-grid scene blocks. Tools/Authoring/RebuildTutorialUnitGrid.py guards the migration using its pre-change snapshot.


### Tutorial route promotion (2026-09-19)

TitleProfileSlotService is created through its existing EnsureInstance path; no scene/prefab serialized override of its launch target was found. The default newProfileTargetSceneName now names PrototypeTutorialUpgradeScene, with the existing HubIntroProgressGate deciding unfinished-profile resumes. No completion IDs, stored profile progress or new-profile semantics changed. EditorBuildSettings uses the new scene GUID at the old entry index so subsequent indices stay stable. RouteSetLoadManifestBuilderWindow defaults its first-run seed list to the new scene, DarkLord_Tutorial and ProtoTypeHub. The existing generated IntroTutorialLoadManifest asset was not regenerated in this route-only change; rebuilding its preload coverage for the new scene is a loading-performance follow-up. Actual title-to-tutorial-to-boss-to-hub playthrough remains unverified; build and serialized route/portal checks passed.


### Hub intro completion weapon guidance (2026-09-19)

HubIntroAfterDarkLordSequence starts the same-scene HubWeaponDepartureGuide after normal completion and CleanupSequenceState. BeginWeaponGuidance uses the current registered player without the unarmed-departure speech. The existing grave/chest owner-scoped white outline and authored bouncing arrow remain active until equipment or guide disable. Already-equipped players skip guidance; canceled/skipped intros never start it. Guide target selection and cleanup remain owned by HubWeaponDepartureGuide (see InventoryAndChestUIStructure.md).

### Current tutorial combat presentation

- `PrototypeTutorialUpgrade.OnExecutionStarted` supplies `Dash2D.TutorialForward` only during dodge stage 1. `AbilityLogic_Dash2D` consumes it before sampling movement, uses world +Y, and clears it in dash cleanup. Ordinary stages retain their movement direction.
- Skill credit and `ProgressText` advance from four right-click full-charge kills to three Q kills; early Q kills do not count.
- `LevelHudPresenter` resolves the scene tutorial on enable/scene load and hides only the above-player reward prompt through stage 3. The reward state and HUD border remain independent.
- The tutorial projectile sweep emits one gold `Evade` on its first successful dash avoidance, including the final swept dash frame.

### Inventory lesson after chest close

- Stage 4 completes on successful chest selection commit. Stage 5 waits until the shared InventoryScreen is no longer active, including its close animation. `PrototypeChestNavigation` resolves the existing `InventoryOpenHudButton` and `InventoryUIOpenRequestHandler` and reuses its four authored shades, glyph and label to spotlight the lower-left inventory HUD.
- The overlay owns a temporary zero time scale, weapon-input block, and dash/swap/interact/consumable press blocks. InventoryToggle remains unblocked; the ordinary InventoryUIManager opens it. The transparent full-screen shield intercepts pointer clicks, so the lesson is completed with the mapped inventory key (default V).
- A fresh InventoryToggle press plus an actually open inventory is required. A rejected open or an already-open screen without that press leaves stage 5 unchanged. Completion advances exactly once to stage 6, drops pointer interception immediately, fades the existing overlay, and releases only this guidance owner's pause/input blocks. Player loss, disable and leaving stage 5 also release them.
- `TutorialGuidanceRecoveryRegression.cs` covers chest ownership, pre-cue dash block cleanup/rebind, sequential objective glyphs, stage guards and inventory open confirmation. `TutorialGuidanceSceneValidation.py` checks local references, disabled fixed guides, the four authored movement glyphs and existing Galmuri black-outline materials. Full-scene visual acceptance remains separate from isolated regression.


### Overhead prompt priority (2026-09-20)

- PrototypeTutorialPromptView, WeaponSwapHintPresenter and LevelHudPresenter submit their authored rectangles and unstacked positions to PlayerOverheadPromptLayout, a layout-only helper in PrototypeTutorialPromptView.cs within the existing UI assembly. Bottom-to-top order is tutorial, Tab weapon swap, level-up. Current screen-space rectangle heights account for the enlarged movement glyph cluster, canvas transforms and pivots; rows have a 12-pixel gap.
- Each submission reflows every visible row from its base position, independent of presenter LateUpdate ordering. Hide/disable removes that owner's entry immediately; inactive/destroyed owners and canvases are pruned. Subsystem registration clears references for domain-reload-disabled Play Mode. No runtime UI creation, new manager, serialized field or scene migration is required.
- Gameplay stage transitions and existing reward availability gates remain owned by their presenters/director. A hidden tutorial cue still advances normally. The layout does not grant reward access or unlock weapon swap.
- Extension point: the three priority slots and presenter submissions define this intentionally bounded layout. There is one active presenter per kind in the current UI root/scene. Full-scene visual acceptance remains a manual check; isolated layout coverage is in Tools/Validation/OverheadPromptLayoutRegression.cs. No Architecture/Contracts promotion is proposed for this small presentation helper.
