# 11. 용어와 기술 참고

[← 시작 페이지](<./00. 절차적 던전 방 제작 툴.md>)

## 용어

| 용어 | 의미 |
| --- | --- |
| 방 템플릿 | 한 방의 레이아웃과 구현 정보를 저장한 `RoomTemplateSO`입니다. |
| 테마 라이브러리 | 같은 보스·복도 테마에서 사용할 방 후보 모음인 `RoomThemeLibrarySO`입니다. |
| 예약 영역 | 방 전체를 감싸며 다른 방과 복도의 침범을 막는 직사각형입니다. |
| 소켓 | 다른 방과 연결될 수 있는 경계의 2칸 출입구 후보입니다. |
| Placement | Monster, Chest, Portal, Prop의 프리팹·위치·연결 데이터입니다. |
| Kill Lock | 지정한 몬스터를 모두 처치할 때까지 보상 상자를 잠그는 구성입니다. |
| Seed | 같은 후보와 설정에서 배치 결과를 재현하기 위한 난수 시작값입니다. |
| Visual Only | 게임플레이 프리팹을 실행하지 않고 레이아웃과 타일만 만드는 미리보기 빌드입니다. |

## 데이터 흐름

`Room Piece Editor → RoomTemplateSO → RoomThemeLibrarySO → DungeonLayoutAssembler → DungeonRoomBuilder → DungeonGenerator`

- `Room Piece Editor`: 제작 복사본을 만들고 검증·저장·미리보기를 제공합니다.
- `RoomTemplateSO`: 예약 크기, 소켓, 타일과 오브젝트 배치를 저장합니다.
- `RoomThemeLibrarySO`: 테마별 방 후보를 제공합니다.
- `DungeonLayoutAssembler`: 테마와 무관하게 후보 방의 배치와 연결을 결정합니다.
- `DungeonRoomBuilder`: 결정된 결과에 타일, 복도, 문과 오브젝트를 구현합니다.
- `DungeonGenerator`: 씬에서 생성 순서와 런타임 설정을 조율합니다.

## 레이아웃 데이터와 구현 데이터

한 방 템플릿 안에 저장되지만 용도는 나뉩니다.

### 레이아웃 조립에 필요한 정보

- Room Id와 역할
- 예약 크기
- 등장 가중치
- 소켓 위치, 방향과 폭

### 방 구현에 필요한 정보

- Floor와 Wall 타일
- Monster, Chest, Portal, Prop 프리팹과 Transform
- Monster에서 Kill Lock Chest로 향하는 연결

배치기는 테마별 규칙을 하드코딩하지 않고, 선택된 라이브러리가 제공하는 방 데이터를 조합합니다.

## 관련 프로젝트 문서

- 단일 페이지 원본 가이드: `Docs/Guides/ContentAuthoring/ProceduralDungeonRoomAuthoringGuide.md`
- 구조와 런타임 책임: `Docs/StructureMemory/ProceduralDungeonRoomPipeline.md`
- 전체 문서 인덱스: `Docs/README.md`

## 주요 소스 위치

- 편집기 UI: `Assets/_Project/Editor/Tools/Dungeon/RoomPieceEditorWindow.cs`
- 방 데이터: `Assets/_Project/Runtime/Features/Map/Procedural/RoomTemplateSO.cs`
- 테마 라이브러리: `Assets/_Project/Runtime/Features/Map/Procedural/RoomThemeLibrarySO.cs`
- 레이아웃 조립: `Assets/_Project/Runtime/Features/Map/Procedural/DungeonLayoutAssembler.cs`
- 방 구현: `Assets/_Project/Runtime/Features/Map/Procedural/DungeonRoomBuilder.cs`
- 런타임 생성: `Assets/_Project/Runtime/Features/Map/Procedural/DungeonGenerator.cs`

기획 작업에는 앞쪽 사용 문서만으로 충분합니다. 이 페이지는 데이터 책임이나 코드 변경 범위를 확인해야 할 때 사용합니다.

