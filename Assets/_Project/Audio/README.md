# Audio Folder Guide

Audio files are grouped by gameplay ownership.

Use:

- `BGM`
- `UI`
- `Player`
- `Monsters`
- `Bosses`
- `Map`

Audio catalog assets belong under `Data/Audio/Catalogs`, not here.

If a clip is referenced by an audio catalog key, move the clip only after confirming the
catalog reference remains valid.
