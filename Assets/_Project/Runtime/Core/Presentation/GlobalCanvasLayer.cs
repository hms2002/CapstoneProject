/// <summary>
/// 책임 : 전역 UI 루트의 공통 캔버스 슬롯을 계층 간 계약 값으로 식별한다.
/// </summary>
public enum GlobalCanvasLayer
{
    GameplayHUD = 0,
    Dialogue = 1,
    Popup = 2,
    Hover = 3,
    Prompt = 4,
    Reward = 5,
    DamagePopup = 6,
    BossHUD = 7,
    GameOver = 8,
    Loading = 9,
}
